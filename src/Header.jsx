import React from 'react';
import './index.css';

const Header = () => {
  return (
    <div className="header">
      <button>로그인</button>
      <button>회원가입</button>
    </div>
  );
};

export default Header;
